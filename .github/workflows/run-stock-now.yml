name: Run stock scanner now

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-stock-now.yml'

permissions:
  contents: read

jobs:
  update:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'KRX refresh disabled pending data-use permission.'
