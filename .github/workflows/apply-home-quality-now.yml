name: Apply Home Quality Now

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-home-quality-now.yml'

permissions:
  contents: read

# The original market-guide placement checks require the suspended KRX section.
# News and SEO workflows continue maintaining the live homepage safely.
jobs:
  home:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy market-guide placement paused while KRX is suspended.'
