name: Refresh stock scanner now

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/refresh-stock-now.yml'

permissions:
  contents: read

jobs:
  refresh:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'KRX refresh disabled while public data service is suspended.'
