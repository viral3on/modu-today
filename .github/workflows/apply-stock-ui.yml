name: Apply stock scanner score guide

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-stock-ui.yml'

permissions:
  contents: read

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'KRX stock UI suspended pending data-use permission.'
